# docker-compose.yml
services:
  jupyter:
    build:
      context: ./jupyter
    container_name: mlops-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/workspace/notebooks
      - ./models:/models
    environment:
      - MODELS_DIR=/models
    restart: unless-stopped